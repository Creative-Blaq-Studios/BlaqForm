---
title: Wizard (Multi-Step Forms)
description: Build multi-step form flows with FkWizard, step-scoped validation, and a progress indicator.
---

`FkWizard` extends the declarative approach of [FkFormBuilder](/guides/form-builder) with step-based navigation. Each step declares which fields it owns, and validation runs only on the current step's fields before advancing.

## Core concepts

- **FkWizardStep** -- an immutable config that declares a step's `title` and `fieldNames`.
- **FkWizardController** -- a `ChangeNotifier` managing the current step index and navigation.
- **FkWizardProgress** -- a visual stepper indicator that rebuilds automatically.

## FkWizardStep

Each step maps to a subset of the fields declared in the `fields` map:

```dart
FkWizardStep(
  title: 'Personal Info',
  fieldNames: ['firstName', 'lastName'],
  subtitle: 'Enter your details',  // optional
  icon: Icons.person,               // optional
)
```

| Property | Type | Description |
|---|---|---|
| `title` | `String` | Label shown in progress indicators. |
| `fieldNames` | `List<String>` | Keys of the fields that belong to this step. |
| `subtitle` | `String?` | Optional extra context text. |
| `icon` | `IconData?` | Optional icon for the step. |

## FkWizardController

The controller is a `ChangeNotifier` that widgets can listen to for rebuilds.

### Properties

| Property | Type | Description |
|---|---|---|
| `currentStep` | `int` | Index of the active step. |
| `stepCount` | `int` | Total number of steps. |
| `currentStepConfig` | `FkWizardStep` | Config object for the current step. |
| `steps` | `List<FkWizardStep>` | All step configs. |
| `canGoBack` | `bool` | `true` if not on the first step. |
| `canGoNext` | `bool` | `true` if not on the last step. |
| `isFirstStep` | `bool` | Whether the current step is first. |
| `isLastStep` | `bool` | Whether the current step is last. |
| `fieldNamesForCurrentStep` | `List<String>` | Field keys for the active step. |

### Methods

| Method | Return | Description |
|---|---|---|
| `goNext()` | `void` | Advance to the next step (no validation). |
| `goBack()` | `void` | Return to the previous step. |
| `goTo(int index)` | `void` | Jump to a specific step (clamped to valid range). |
| `validateAndGoNext(FkFormController)` | `Future<bool>` | Validate current step's fields, advance only if all pass. Returns `true` on success. |

## FkWizardProgress widget

A visual progress bar that auto-rebuilds when the wizard step changes:

```dart
FkWizardProgress(
  controller: wizard,
  activeColor: Colors.blue,    // defaults to theme primary
  inactiveColor: Colors.grey,  // defaults to grey
  lineThickness: 2.0,
  circleSize: 32.0,
)
```

- Completed steps show a checkmark icon.
- The active step shows a filled circle with the step number.
- Future steps show an outlined circle.

## FkWizard widget

The top-level widget that wires everything together:

```dart
FkWizard(
  fields: { /* FkFieldConfig map */ },
  steps: [ /* FkWizardStep list */ ],
  builder: (context, scope, wizard) => /* widget tree */,
  onComplete: (values) async => /* handle submission */,
  autovalidateMode: FkAutovalidateMode.onUserInteraction,
  crossValidators: [],
)
```

The `builder` callback receives three arguments:

1. `BuildContext context` -- the widget's build context.
2. `FkFormBuilderScope scope` -- the same fluent scope from `FkFormBuilder` for rendering fields.
3. `FkWizardController wizard` -- for navigation and step queries.

## Complete example: 3-step onboarding

```dart
class OnboardingWizard extends StatelessWidget {
  const OnboardingWizard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Onboarding')),
      body: Padding(
        padding: const EdgeInsets.all(24),
        child: FkWizard(
          fields: {
            // Step 1: Account
            'email': FkFieldConfig<String>.email(
              label: 'Email',
              validators: [Fk.required(), Fk.email()],
            ),
            'password': FkFieldConfig<String>.password(
              label: 'Password',
              validators: [Fk.required(), Fk.minLength(8)],
            ),
            // Step 2: Profile
            'name': FkFieldConfig<String>.text(
              label: 'Full Name',
              validators: [Fk.required()],
            ),
            'birthday': FkFieldConfig<DateTime>.date(
              label: 'Date of Birth',
              validators: [Fk.required<DateTime>(), Fk.age(18)],
            ),
            // Step 3: Preferences
            'country': FkFieldConfig<String>.dropdown(
              label: 'Country',
              options: ['US', 'UK', 'CA'],
              optionLabels: {
                'US': 'United States',
                'UK': 'United Kingdom',
                'CA': 'Canada',
              },
              validators: [Fk.required()],
            ),
            'newsletter': FkFieldConfig<bool>.checkbox(
              initialValue: false,
              label: 'Subscribe to newsletter',
            ),
          },
          steps: [
            FkWizardStep(
              title: 'Account',
              fieldNames: ['email', 'password'],
              icon: Icons.lock,
            ),
            FkWizardStep(
              title: 'Profile',
              fieldNames: ['name', 'birthday'],
              icon: Icons.person,
            ),
            FkWizardStep(
              title: 'Preferences',
              fieldNames: ['country', 'newsletter'],
              icon: Icons.settings,
            ),
          ],
          onComplete: (values) async {
            await Future.delayed(const Duration(seconds: 2));
            debugPrint('Onboarding complete: $values');
          },
          builder: (context, scope, wizard) {
            return Column(
              crossAxisAlignment: CrossAxisAlignment.stretch,
              children: [
                // Progress indicator
                FkWizardProgress(controller: wizard),
                const SizedBox(height: 32),

                // Step content
                Expanded(
                  child: _buildStepContent(scope, wizard),
                ),

                // Navigation buttons
                const SizedBox(height: 24),
                Row(
                  children: [
                    if (wizard.canGoBack)
                      TextButton(
                        onPressed: wizard.goBack,
                        child: const Text('Back'),
                      ),
                    const Spacer(),
                    if (!wizard.isLastStep)
                      ElevatedButton(
                        onPressed: () => wizard.validateAndGoNext(
                          scope.formController,
                        ),
                        child: const Text('Next'),
                      ),
                    if (wizard.isLastStep)
                      scope.submitButton('Finish'),
                  ],
                ),
              ],
            );
          },
        ),
      ),
    );
  }

  Widget _buildStepContent(FkFormBuilderScope scope, FkWizardController wizard) {
    switch (wizard.currentStep) {
      case 0:
        return Column(children: [
          scope.email('email'),
          const SizedBox(height: 16),
          scope.password('password'),
        ]);
      case 1:
        return Column(children: [
          scope.text('name'),
          const SizedBox(height: 16),
          scope.date('birthday'),
        ]);
      case 2:
        return Column(children: [
          scope.dropdown<String>('country'),
          const SizedBox(height: 16),
          scope.checkbox('newsletter'),
        ]);
      default:
        return const SizedBox.shrink();
    }
  }
}
```

## Related

- [Form Builder](/guides/form-builder) -- the single-page equivalent
- [Validation](/guides/validation) -- validators used in each step
- [Indicators](/guides/indicators) -- FkFormProgress for overall completion tracking
